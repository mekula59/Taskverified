# TaskVerified

TaskVerified is a human-verified micro-work platform for startups and communities.

This repo is currently in a cleanup-and-scaffold phase. The app shell is organized around the real MVP shape:

- `public`
- `worker`
- `poster`
- `shared`

The current code intentionally stops at a stable shell, route map, and planning docs so implementation can continue from a cleaner base.
